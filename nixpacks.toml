[phases.install]
cmds = ["/opt/venv/bin/pip install -r requirements.txt"]

[phases.build]
cmds = ["/opt/venv/bin/playwright install --with-deps chromium"]

[start]
cmd = "/opt/venv/bin/python server.py"
